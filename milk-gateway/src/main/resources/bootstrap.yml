server:
  port: 10000
spring:
  application:
    name: milk-gateway
  cloud:
    nacos:
      discovery:
        server-addr: 47.99.190.75:8848
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        file-extension: yml


