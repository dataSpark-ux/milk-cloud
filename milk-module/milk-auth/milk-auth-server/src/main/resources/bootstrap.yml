server:
  port: 10001
spring:
  application:
    name: milk-auth
  cloud:
    nacos:
      discovery:
        server-addr: 47.99.190.75:8848
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        file-extension: yml


